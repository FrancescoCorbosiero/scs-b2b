{% extends 'layout.twig' %}

{% block title %}{{ t('error.500_title') }}{% endblock %}

{% block content %}
    <div class="max-w-md mx-auto mt-16 text-center">
        <p class="text-6xl font-black text-neutral-300 mb-4">500</p>
        <h1 class="text-xl font-bold mb-2">{{ t('error.500_title') }}</h1>
        <p class="text-neutral-500 mb-6">{{ t('error.500_text') }}</p>
        <a href="/" class="inline-block rounded-lg bg-neutral-900 text-white px-5 py-2.5 font-semibold hover:bg-neutral-700">{{ t('error.back_home') }}</a>
    </div>
{% endblock %}
